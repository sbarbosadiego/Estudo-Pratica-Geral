var velocidade = 60.9

if (velocidade > 60.0) {
    console.log(`Foi ultrapassado o limite de velocidade de 60km/h \nVelocidade atual: ${velocidade}`)
} else {
    console.log(`Você está dentro do limite de velocidade \nVelocidade atual: ${velocidade}`)
}
